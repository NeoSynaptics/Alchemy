"""AlchemyBrowser — link-less semantic research module.

User gives a question, not a URL. The pipeline decomposes the query into
parallel searches, fetches pages, extracts content, scores relevance,
and synthesizes a structured answer with source attributions.
"""
