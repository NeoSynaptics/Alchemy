"""AlchemyAssets — first-class client-side SDK packages.

Reusable packages for any app that connects to Alchemy.
Each sub-package is framework-agnostic and ships as an npm package.

Packages:
    connect/   — @alchemy/connect  WebSocket client (text + binary)
"""
